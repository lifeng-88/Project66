# 此文件为 vivide 子目录独立仓库时的副本。
# 当前 monorepo 根目录请使用：../../.github/workflows/ios-release.yml

name: iOS Release (vivide subdir)

on:
  workflow_dispatch:

jobs:
  redirect:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "::error::请使用仓库根目录 .github/workflows/ios-release.yml 触发 Release 打包"
          exit 1
